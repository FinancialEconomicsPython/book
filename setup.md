# 설치 및 사용법

파이썬을 처음 접하는 독자도 쉽게 사용할 수 있도록 **Google Colab 사용을 권장**합니다.

## 코드 실행하지 않고 보기만 하는 경우

GitHub 저장소의 `chapters/` 폴더에서 `.ipynb` 파일을 클릭하면 코드와 결과를 바로 볼 수 있습니다.

- 👉 [GitHub 저장소의 chapters 폴더 바로가기](https://github.com/FinancialEconomicsPython/book/tree/main/chapters)

---

## 코드를 직접 실행하는 경우

대부분의 노트북은 다음과 같은 순서로 구성되어 있습니다:

1. 필요한 패키지 설치
2. 데이터 불러오기
3. 그림 및 실증 분석 재현

```{warning}
노트북을 실행하기 전에 반드시 **1단계: 실행 환경 준비**를 먼저 수행하세요!
```

---

## 1단계: 실행 환경 준비 (⚠️ 필수)

### utils 폴더의 중요성

이 저장소의 대부분의 Jupyter Notebook은 `utils/` 폴더에 포함된 **공통 함수**를 사용합니다:

- 그래프 작성 (`plot_utils.py`)
- 데이터 처리 (`nber_utils.py`)
- 환경 설정 (`preamble_core.py`)

```{attention}
GitHub에서 노트북을 Google Colab으로 열 경우, 노트북 파일(`.ipynb`)만 복사되고 **`utils/` 폴더는 자동으로 포함되지 않습니다.**
```

따라서 아래의 **방법 A 또는 방법 B 중 하나를 반드시 선택**하여 `utils/` 폴더를 작업 환경에 포함시켜야 합니다.

---

### ✅ 방법 A (권장): 저장소 전체를 Colab에 clone하기

가장 안정적인 방법이며, 모든 노트북이 **저자 의도대로 동일하게 동작**합니다.

#### 단계별 안내

1. Google Colab에서 노트북을 엽니다
2. **노트북 맨 위에 새 코드 셀을 추가**합니다 (상단 `+ 코드` 버튼)
3. 아래 코드를 입력하고 실행합니다:

```python
# FinancialEconomicsPython 저장소를 Colab 환경으로 복제
!git clone https://github.com/FinancialEconomicsPython/book.git

# 작업 디렉토리를 저장소 루트로 이동
%cd book
```

4. 이제 BASE와 API 키만 설정하면 됩니다:

```python
BASE = "/content/book"  # ← 이 경로 사용
key_api_ECOS = "여기에_본인의_API키_입력"
```

#### 방법 A의 장점

- ✅ `utils/` 폴더가 자동으로 포함됨
- ✅ `chapters/`, `readings/` 등 상대경로가 모두 정상 작동
- ✅ 저장소 업데이트 내용도 쉽게 반영 가능

---

### ✅ 방법 B: utils 폴더의 파일을 수동으로 다운로드

방법 A가 작동하지 않을 경우 이 방법을 사용하세요.

#### 단계별 안내

1. [GitHub 저장소의 utils 폴더](https://github.com/FinancialEconomicsPython/book/tree/main/utils)로 이동합니다

2. 다음 파일들을 다운로드합니다:
   - `nber_utils.py`
   - `plot_utils.py`
   - `preamble_core.py`

3. Google Colab의 왼쪽 **📁 파일** 탭을 클릭합니다

4. `utils/` 폴더를 만들고 다운로드한 파일들을 업로드합니다

5. 최종 파일 구조가 아래와 같아야 합니다:

```
/content/
├── your_notebook.ipynb
└── utils/
    ├── nber_utils.py
    ├── plot_utils.py
    └── preamble_core.py
```

---

## 2단계: 노트북 실행하기

### 🔹 방법 1: GitHub에서 직접 열기

1. [GitHub 저장소의 chapters 폴더](https://github.com/FinancialEconomicsPython/book/tree/main/chapters)로 이동
2. 실행하고 싶은 장의 `.ipynb` 파일을 클릭
3. 파일 상단의 안내에 따라 `BASE`와 `key_api_ECOS` 설정
4. 셀을 위에서부터 차례대로 실행 (단축키: `Shift + Enter`)

---

### 🔹 방법 2: Google Colab에서 열기

1. [Google Colab](https://colab.research.google.com)에 접속
2. 상단 메뉴: **파일 → 노트북 열기 → GitHub**
3. 저장소 이름 입력: `FinancialEconomicsPython/book`
4. 원하는 `.ipynb` 파일 선택
5. 셀을 위에서부터 차례대로 실행 (단축키: `Shift + Enter`)

```{tip}
Colab의 무료 버전으로도 대부분의 노트북을 실행할 수 있습니다.
```

---

### 🔹 방법 3: 로컬 환경에서 실행 (고급 사용자)

로컬 환경에서 파이썬을 사용하는 독자는 다음과 같이 사용할 수 있습니다:

1. 저장소를 `git clone`하거나 ZIP 파일로 다운로드
2. Jupyter Notebook 또는 JupyterLab에서 `.ipynb` 파일 실행
3. `utils/` 폴더가 같은 디렉토리에 있어야 함

```bash
# 터미널에서 실행
git clone https://github.com/FinancialEconomicsPython/book.git
cd book
jupyter notebook
```

```{warning}
이 방법은 파이썬 환경 설정에 익숙한 독자에게만 권장됩니다.
```

---

## 한국은행 ECOS API 키 발급받기

대부분의 노트북에서 한국은행 데이터를 사용합니다. API 키를 발급받는 방법:

1. [한국은행 ECOS 사이트](https://ecos.bok.or.kr/api/#/) 접속
2. 회원가입 후 로그인
3. 'API 신청' 메뉴에서 인증키 발급
4. 발급받은 키를 노트북의 `key_api_ECOS` 변수에 입력

```python
key_api_ECOS = "여기에_발급받은_API키_붙여넣기"
```

---

## 문제 해결 (Troubleshooting)

### Q: "ModuleNotFoundError: No module named 'utils'" 에러가 발생합니다

**A:** 1단계의 방법 A 또는 방법 B를 다시 확인하세요. `utils/` 폴더가 올바른 위치에 있어야 합니다.

---

### Q: API 키 관련 에러가 발생합니다

**A:** `key_api_ECOS` 변수에 실제 API 키를 입력했는지 확인하세요. `"YOUR_ECOS_API_KEY_HERE"`를 그대로 두면 에러가 발생합니다.

---

### Q: Google Drive 마운트 에러가 발생합니다

**A:** 방법 A를 사용하면 Google Drive 없이도 실행할 수 있습니다. BASE 경로를 `/content/book`으로 설정하세요.

---

## 추가 도움말

더 자세한 내용은 [GitHub 저장소의 README](https://github.com/FinancialEconomicsPython/book)를 참고하거나, [financialeconomicspython@gmail.com](mailto:financialeconomicspython@gmail.com)으로 문의하세요.
